Оглавление

	Версии баз

	Развёртывание

Версии баз

	postgres 9.5

	mysql 5.7

Развёртывание

	Убедиться, что стоит pip для python 2

		pip --version должен вывести ...from /usr/lib/python2.7/dist-packages (python 2.7)

		если нет, то sudo apt-get install python-pip

	Убедиться, что стоит virtualenv

		если нет, то sudo pip install virtualenv

	Переходим в директорию с проектом 

		python create_bootstrap.py

		должен появиться bootstrap.py

		python bootstrap.py

		должна появиться директория bin

	Устанавливаем нужные версии субд

	Активируем виртуальное окружение

	Устанавливаем зависимости

		Перед установкой убедиться, что есть:

			- права в gitlab на клонирование пакетов

			- системная библиотеки libxml2 and libxslt

				sudo apt-get update

				sudo apt-get install libxml2-dev libxslt-dev python-dev

	Создаем нужные бд

	Миграции

		Перед выполнением убедиться, что есть:

			- драйвер MySQLdb

				sudo apt-get update

				sudo apt-get install python-mysqldb

				pip install mysql

		Выдаст ошибку в конце:

			File "/home/ilya/work/projects/magic-env/local/lib/python2.7/site-packages/django/contrib/sites/models.py", line 34, in _get_site_by_id
		    if site_id not in SITE_CACHE:
			TypeError: unhashable type: 'dict

			Убираем 

	Переходим в имя_проекта/conf, открываем файл settings_local.py.template, копируем все

	В имя_проекта/conf созаем файл settings_local.py и вставляем все скопированное

	Создаём необходимые бд

		Одну postgres и mysql. ()

	Вопросы

		Где коннекты к постгрес?

		Нужен ли кусок в /magic/modules/configurator/admin.py на строке 151? (Создаем формы конфигураторов для тестов)

	Не работало

		ALLOWED_HOSTS

		маршрутизация баз данных







