Изучить

	nginx

		связка в одном контейнере nginx + wsgi/gunicorn + django/flask

		связка в разных контейнерах nginx + wsgi/gunicorn + django/flask

	async

		Twisted (книга)

		Кооперативная многозадачность

		Соопрограммы и генераторы python (коротко)

		Asyncio

Транзакции

Выборки

Insert From Select

Основы администрирования

Как работает

Lateral JOIN

View

----------------------

web app servers отличия

wsgi

uwsgi

sapi

redirect proxy отличия

----------------------

SQL
	нормальные формы, нормализация, консистентность данных

	ключи и соединения таблиц (всякий JOIN), соединения запросов (UNION, INTERSECT, EXCEPT)

	подзапросы

	индексы

СУБД

	postgresql: вьюхи, курсоры, индексы, ограничения, хранимые процедуры, триггеры, windows-функции (хотя бы обзорно)

	уровень администрирования: уметь завести пользователя и дать права, понимать, где лежат настройки, где база, где логи; что в postgresql.conf, что в pg_hba.conf;

	бекапы и восстановление

	DML, DQL (select-insert-update-delete)

	транзакции, уровни изоляции

	профилирование, EXPLAIN, оптимизация запросов

Основы веб:

	Как работает веб в общем:

		как обрабатывается запрос в вебе (примерно что ты мне рассказывала, может, чуть-чуть подробнее)

		раздача статических файлов, что такое CDN

		сессии и куки, как, что, зачем (в общих чертах, факультативно)

Веб-сервер:

	зачем вообще нужен веб-сервер; apache, nginx

	как идет запрос от браузера пользователя до конечной точки, что и как возвращается?

	чем характеризуется http-запрос (параметры)?

	схема URI (и что такое sheme, authority, path, ...)?

	чем URI отличается от URL

	прокси и редирект, чем отличается?

	сокеты и порты (очень поверхностно, рассказать на уровне полиморфизма с коржиком)

Nginx:

	настройка доступа, allow, deny

	redirect и rewrite

	301 и 302 редирект, отличия;

	proxy_pass

	опционально - че он в общих чертах умеет и как его грамотно настраивать (с ruhighload)

	upstream

DNS:

	что такое DNS и зачем нужен?

	доменные зоны, домен первого, второго и т.п. уровней;

	как идет поиск поддомена

	файлы hosts

	TTL

	запись типа A

	запись типа NS

	прокси и редирект

	SAPI - что такое вообще, как связано с wsgi;

	wsgi - стандарт и его реализации; CGI, FastCGI, uWSGI, ...

	связь веб-сервера (nginx) и application сервера

	обратный прокси

