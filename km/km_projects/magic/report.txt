Иногда Александру Глубокову нужен ежемесячный отчет.

Этот отчет формируется по крону 1-го числа каждого месяца.

Бывает отчет по korallmicro и snr.

Для того чтобы сформировать отчет руками в любое время нужно:

	Перейти на боевой magic

		ssh devel@192.168.7.76

		mc

		~/www/magic

	Зайти в shell

		bin/manage shell

	snr

		from cards.tasks import *

		task = ReportMasterSnr()

		task.delay()

	korallmicro

		Примечание! По кораллу не делал, поэтому может не рабатать.

		from cards.tasks import *

		task = ReportKorallmicro()

		task.delay()